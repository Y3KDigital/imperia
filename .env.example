# K IMPERIA Genesis Intake - Environment Configuration
# Copy this file to .env and fill in your actual values

# SendGrid API Key (get from: https://app.sendgrid.com/settings/api_keys)
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxx

# Email Configuration
FROM_EMAIL=genesis@unykorn.io
FROM_NAME=K IMPERIA Genesis

# Internal Team Recipients (comma-separated)
INTERNAL_RECIPIENTS=kevan@unykorn.io,jimmy@unykorn.io,thomas@unykorn.io,isaac@unykorn.io

# Server Configuration
PORT=3000
NODE_ENV=production

# Admin Access Token (change in production!)
ADMIN_TOKEN=your_secure_admin_token_here

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_KEY=your_service_role_key_here

# CORS Configuration (for frontend)
ALLOWED_ORIGINS=http://localhost:8000,https://ipfs.io,https://gateway.pinata.cloud

# Base URL for referral links (update after IPFS deployment)
BASE_URL=https://ipfs.io/ipfs/YOUR_CID/index.html

# Optional: Database connection (for future backend storage)
# DATABASE_URL=postgresql://user:pass@localhost:5432/k_imperia
